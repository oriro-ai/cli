apiVersion: v1
kind: Service
metadata:
  name: oriro
  labels:
    app: oriro
spec:
  type: ClusterIP
  selector:
    app: oriro
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP

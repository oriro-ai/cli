# Oriro GMI Cloud Provider

Official Oriro provider plugin for hosted GMI Cloud models through an
OpenAI-compatible API.

Install from Oriro:

```bash
oriro plugins install @oriro/gmi-provider
oriro gateway restart
```

Configure a GMI Cloud API key, then select models with refs such as
`gmi/google/gemini-3.1-flash-lite`.

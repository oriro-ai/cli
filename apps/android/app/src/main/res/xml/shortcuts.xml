<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_ORIRO"
        app:queryPatterns="@array/ask_oriro_query_patterns">
        <intent
            android:action="ai.oriro.app.action.ASK_ORIRO"
            android:targetPackage="ai.oriro.app"
            android:targetClass="ai.oriro.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>

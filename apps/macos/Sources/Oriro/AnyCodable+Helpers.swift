import Foundation
import OriroKit

// Prefer the OriroKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = OriroKit.AnyCodable
typealias InstanceIdentity = OriroKit.InstanceIdentity
